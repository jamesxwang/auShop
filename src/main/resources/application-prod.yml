server:
  port: 443
  context-path: /
  tomcat:
    uri-encoding: UTF-8
  ssl:
    key-store: classpath:www.amazingxu.xyz.jks
    key-store-password: XXff05141125
    enabled: true

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/wxblog_dev?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8&autoReconnect=true&failOverReadOnly=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  http:
    encoding:
      force: true
      charset: utf-8
      enabled: true
